apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svc
  namespace: {{ .Values.namespace }}
  labels:
     app: {{ .Values.name }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.port }}
      protocol: TCP
  selector:
     app: {{ .Values.name }}
